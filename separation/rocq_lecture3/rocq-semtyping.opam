opam-version: "2.0"
maintainer: "Robbert Krebbers"
authors: "Robbert Krebbers"

synopsis: "Semantic typing in separation logic"

depends: [
  "rocq-iris-heap-lang" { (= "dev.2026-01-07.0.f12346d8") | (= "dev") }
]

build: [make "-j%{jobs}%"]
install: [] # This repo does not install
